# 现场记忆手账 — 项目上下文

> 【AI 接手必读】在你做任何操作之前，请完整读完本文件。
> 本文件由 AI 维护，每次完成任务后必须同步更新。

---

## ⚡ AI 接手 Checklist（必须逐项确认）

- [ ] 我已读完"产品定位"，理解这是**实体文创生意**，不是纯软件项目
- [ ] 我已读完"当前状态快照"，知道现在做到哪一步了
- [ ] 我已读完"技术架构"，知道两个工具目录各自负责什么
- [ ] 我已读完"关键设计决策"，不会推翻已有的技术选型
- [ ] 我已读完"已知问题"，不会踩已记录的坑（尤其是分辨率门禁和 SVG 外链）
- [ ] 我已读完"商业验证状态"，知道**当前最该做的不是写代码**
- [ ] 我已读完"协作约定"，知道完成任务后需要做什么

---

## 产品定位

把顾客的**现场照片**（生日、演出、旅行、活动）转成「水粉剪纸拼贴风」的贴纸元素，排成 A5 版面，送数码模切印成**实体手账贴纸套装**。

核心差异化：**不是给一张 AI 艺术画，而是从那天的照片里把值得留存的物件抠出来做成能贴的贴纸。**

目标用户：小红书上做手账、有纪念需求的年轻女性。

定价（未验证）：1 张照片 59 元 / 2 张 99 元 / 3 张 129 元 / 4 张 149 元。

⚠️ **这是一门手艺生意，不是能靠工具规模化的生意。** 详见"竞争态势"。

---

## 当前状态快照

- **最后完成事项：** v3.4.1 修 3 个缺陷 + 06 银杏重跑交付 → `run_v37/p6/`、`交付_v37_20260901/06_银杏_v37厂家文件/`。`tests/` 101 → **128 项全绿**。详见 CHANGELOG 2026-09-01 · v3.4.1
  - ✅ **G0 截断 JSON 有限重试**（3 次，重试时追加「只输出闭合 JSON」提示，超限才退出且报错点明 G0 阶段）
  - ✅ **断点续跑**（`resume_state.json` + prompt 哈希校验，G0 与已过质检的轮次复用、日志列出复用项，`--fresh` 强制重跑）+ 生图瞬时错误按 2/4/8 秒指数退避重试 3 次
  - ✅ **G0 召回下限**：`standalone_objects` 要求 ≥10 项且跨类别，不足最多补问 2 次
  - **06 银杏验收通过**：ginkgo leaf / backpack / baseball cap / coat / stone pillar + 人物 = 5 物品 + 1 人物，银杏叶在位；共 5 轮、无人工干预；印前 400dpi / 刀线 6 / 必须修 0 / 高危 0
- **上一次完成事项：** v3.4.0 红队评审 R1~R5 整改 + IP 合规策略收紧（**未跑生图**）：收敛保证（不可收敛检测 + 换元素，不再靠堆词库）、dpi 全部改公式推导 + 能力探测与显式降级、provider 免费自检 fail-fast、模切店须知补色彩管理、.gitignore 隐私实测加固；IP 层新增「现代地标建筑本体一律剔除 / 随身物品最高优先级」。`tests/` 48 → **101 项全绿**。详见 CHANGELOG 2026-09-01 · v3.4.0
  - ⚠️ 本轮改动全部**未经真实出图验证**（下一轮跑图时验）
- **上一次完成事项：** v3.3.4（**未跑生图**）：修「生图产物认领不唯一」与「同族去重被兜底回填绕过」两个缺陷，并处理三条存疑项（卡纸分行排序写死的 6 / 两套面积阈值 / board·screen 误命中案板屏风）；`tests/` 29 → **48 项全绿**。详见 CHANGELOG 2026-08-30 · v3.3.4
  - ⚠️ 修复尚未经真实出图验证（本轮明确不跑图）
- **更早：** 技术债集中清理（v3.3.3，**未跑生图**）：EXIF 方向、总枚数硬门禁、同族词库（树体/建筑/花部件-整体关系）、低价值空色块件降权四个缺陷全部修完，另修 8 处隐患；`tests/` 11 → **29 项全绿**
- **更早：** 真实出图验证 4 单 + v2 交付包上传（v3.2）
- **下一步任务：** ❗**商业验证 —— 找到 ≥3 个愿意真付 99 元的人**，不是继续开发
- **AI 出图产线：** ✅ 全流程可用（G0 体检 → **选品** → 出图 → 重排 → 双重质检 → 刀线）
- **已交付的 4 单成品：** ✅ v2 版已重跑并经独立视觉复核，缺陷全部修复，可直接送厂
- **印前质检：** ✅ 可用（分辨率/邻距/边距/最细笔画/刀线）
- **A5 单版刀线 SVG：** ✅ 可用
- **A3 四宫格拼版：** ✅ 可用
- **数码模切导出：** ✅ 可用（单枚透明 PNG / PDF / SVG）
- **gpt-image-2 适配：** ⚠️ **代码完成但从未实测**（当前环境无真实 OpenAI key）
- **火山方舟 Seedream：** ⚠️ 代码完成未实测（端点连通性已验证，返回标准 401）
- **商业验证：** ❌ **零付费用户，从未真实卖出一单**

---

## 技术架构

```
memory-sticker-forge/          # AI 出图产线
  forge.py                     # 主入口：G0体检→出图→重排→双重质检→定向重试
                               #   默认 --max-rounds 4，--figure-style collage
                               #   --gap 8 --margin 10 --dpi 400
  providers.py                 # ★ AI 能力适配层（迁移隔离点）
                               #   generate_image(photo, prompt, out_png)
                               #   analyze_images(paths, task)
                               #   支持 openai / volcengine / cmd（接任意外部命令）
  tools/selftest_provider.py   # provider 三级自检（免费干检查→视觉→生图）
  forge_a3.py                  # 探索性：一张照片出 24 枚（非主线）
  probe_capacity.py            # 探索性：探测一张照片能出多少图案（非主线）

print-ready-doctor/            # 印前工具链
  print_ready_doctor.py        # 量化质检 + 产出 cutline.svg / cutline_only.svg
  relayout.py                  # 程序化 A5 网格重排（硬保证邻距与边距）
  impose_a3.py                 # A3 四宫格拼版 + 裁切标记 + kiss cut
  embed_svg.py                 # ★ 把外部位图 base64 内嵌进 SVG（防丢图）
  export_for_digital_cut.py    # ★ 数码模切导出（单枚透明PNG + PDF）
  requirements.txt
```

**关键：`forge.py` 不直接调用任何具体平台的能力，全部走 `providers.py`。** 换后端只需实现两个 provider 函数。

环境变量：
```bash
FORGE_PROVIDER=openai|volcengine|cmd      # 生图+视觉
FORGE_IMAGE_PROVIDER / FORGE_VISION_PROVIDER  # 可分开配
OPENAI_IMAGE_MODEL=gpt-image-2             # 默认
OPENAI_IMAGE_SIZE=a5-300                   # 1760x2480；a5-400=2336x3312
ARK_API_KEY / ARK_IMAGE_MODEL / ARK_IMAGE_SIZE
PRINT_READY_DOCTOR_DIR / DOCTOR_PATH / RELAYOUT_PATH  # 工具路径覆盖
CJK_FONT_PATH                              # 中文字体
```

---

## 关键规格（不要改动）

| 项 | 值 | 原因 |
|---|---|---|
| 成品尺寸 | A5 148×210mm | 印刷厂标准，4 张拼 A3 |
| 分辨率 | 400dpi = 2331×3307px | 由 `px_at(mm,dpi)=round(mm÷25.4×dpi)` 推导，不写魔数；provider 不够时显式降级到 300dpi 并标注 |
| 生图最小短边 | **1739px** | = 300dpi 理论值 1748px × (1−0.5% 量化容差)，低于此 `providers.py` 直接报错 |
| 每版枚数 | 6 枚 = 5 物品 + 1 人物 | 一张照片的合理容量 |
| 单枚尺寸 | 22~62mm | 手账贴纸合理区间 |
| 元素最小净距 | 8mm | 模切安全 |
| 四边留白 | 10mm | 工艺只需 3mm，10mm 是为了视觉均衡 |
| 刀线 | 图层 `CutContour`，#FF00FF，0.25pt | 只切不印 |
| 切割方式 | 半切 kiss cut | 底纸不断，可逐枚撕取 |

---

## 关键设计决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 人物画法 | `collage` 分色拼贴（默认） | 无脸但有头发/肤色/衣服色。旧的 `silhouette` 单色剪影被用户否掉（"棕色，不好看"） |
| 排版方式 | 程序化重排，不用模型排版 | 模型排版无法保证邻距/边距，`relayout.py` 硬保证 |
| 质检 | 量化 + 目视双重 | 量化查数值，目视查"物品是否丢失" |
| 物品清单 | G0 显式提取后写进 prompt | 否则模型会只画人物，丢掉吉他/鼓等物品 |
| 第三方 IP | G0 检测 + 生成阶段强制剔除 + 复检 | 已实测能去掉 Fender/Orange/Dixon logo、知乎水印 |
| 重试 | 有限次（默认 4 轮）+ 定向重试 | AI 出图有 token 成本，不能无限重试 |
| 选品 | `select_objects()` 三级过滤，不用 `objs[:n]` | 直接截断会丢纪念物、会同时选中容器与内容物、会选中同族物品。详见 v3.1.1 |
| 同族判定 | 中心词（最后一个词） | 子串匹配会把 microphone 判成 phone、guitar amplifier 判成 guitar |
| 成套装备 | 自动拆成单件（drum kit→bass drum） | 整套装备靠细杆支撑，必然同时触发"过细"和"带支架"，无论重试多少轮都不收敛 |
| 分辨率门禁 | 输入端 `--min-input-dpi 300` | 低分辨率图会被插值放大成"假 400dpi" |
| AI 能力隔离 | `providers.py` 适配层 | 为迁移 Codex 准备，主逻辑不碰内部 skill |
| SVG 交付 | 必须内嵌位图 | 外链版单独发工厂会丢图 |
| 数码模切 | 同时给 PDF 和单枚透明 PNG | 店家流程分两种，先问再发 |

---

## 商业验证状态（❗最重要的一节）

**当前：零付费用户。**

已定的验证线：**≥3 人真付 99 元才继续投入。**

已排除的错误做法：
- ❌ 把小红书点赞/收藏当付费验证
- ❌ 卖数字素材包（skill 已开源，且很少人自打印）
- ❌ 伪造稀缺（"限量 X 单"）—— 没有真实限制就不能说
- ❌ 做冰箱贴（样品不像、周期 8 天、卡纸磁铁找不到）

成本已核实：
- A3 不干胶模切（=4 张 A5）**20 元**
- 快递 8 元 / 包装 4 元 / token 约 1 元每张
- 结论：1 张档 59 元时薪太低，**2 张 99 元最适合做付款验证**

---

## 竞争态势（2026-08 更新）

**OpenAI 于 2026-08-24 发布 ChatGPT 自定义贴纸包功能**（手机 App，单次最多 9 枚，透明底 PNG，免费用户灰度中）。

影响评估：
- ❌ "分辨率不够印"这条护城河**已不成立**：1024px 印 6cm 贴纸等效 433dpi，绰绰有余
- ⚠️ 剩下的鸿沟（出血/刀线/CMYK/拼版）都是 **know-how 类壁垒，半衰期以月计**
- ✅ 仍有效的三条：**大陆不可用**（租来的时间，非能力）、**策展**（AI 不知道哪个物件对顾客有意义）、**交付确定性**
- 📌 行动：文案必须从"AI 生成"改为"能贴的成品"；可开"你出图我帮你印"低价档接流量

---

## 已知问题 / 技术债

- ⚠️ **本轮所有改动都没跑过真图**：收敛保证、IP 剔除、300dpi 降级三条都只有离线测试和假 provider 冒烟验证过
- ⚠️ **provider 实测状态（不要美化）**：运行版自带的 provider ✅ 已实测（该实现不在本副本内）；**gpt-image-2 / 火山 Seedream / Gemini 三个全部 ⚠️ 从未真实调用过**，`cmd` 取决于你自己接的命令。`providers.py --selftest` 只能证明「配置层面没坑」，证明不了能出图、不会被内容审核拒、计费正常。Seedream 模型 ID 带日期后缀（`-250828`），报 `model not found` 时先去控制台抄最新 ID
- ⚠️ **色差从未实测**：交付是 RGB，模切店须知里已写清 sRGB 解释 + 打样要求 + 易偏色区域，但**没有任何实物色卡比对数据**
- ⚠️ **词库仍在（作为先验），只是不再是唯一依据**：`COMPOSITE_REPLACE`(13) / `SIMILAR_FAMILIES`(17) 遇新品类照旧漏判，兜底靠「模型判断 + 通用中心词规则 + 不可收敛就换元素」
- ✅ **已解决（v3.4.1）· G0 截断 JSON 整单崩**：`_g0_ask_with_retry()` / `g0_parse_problem()` 对空返回/非 JSON/未闭合/顶层非对象做有限 3 次重试，超限报错点明 G0 阶段。回归测试 4 项
- ✅ **已解决（v3.4.1）· 无断点续跑、生图一次报错整单重来**：`resume_state.json` + `prompt_sig()` 哈希校验，G0 与已完成轮次可复用且日志逐项列出，`--fresh` 强制重跑；`generate_image()` 对瞬时错误按 2/4/8 秒退避重试 3 次，永久错误（key/模型/审核/余额/分辨率）不重试。回归测试 13 项
- ✅ **已缓解（v3.4.1）· G0 召回抖动导致主角缺席**：G0 要求 ≥10 项且跨类别，不足最多补问 2 次并只做增量合并。回归测试 6 项。**这是降低概率不是消灭概率**，主角缺席仍要人眼验收，兜底靠 `--objects` 人工指定
- ⚠️ **视觉模型返回形状不稳定**：已在 G0 出口和质检入口做形状归一（`_strlist` / `_pairs_from`），原则是「格式抖动只降级成漏判，不允许崩产线」；代价是可能静默丢掉解析不了的字段，排查时要看原始返回

- ✅ **已解决（v3.3.4）· 生图产物认领不唯一**：`generate_image()` 调用 provider 前先删掉同名旧文件，避免生图失败时把上一次的产物当成本次结果交付；`cmd` provider 保持「只写 `{out}`、不扫描目录」的约定。公开版三个 provider（`openai` / `volcengine` / `cmd`）都直接写目标路径，不存在「扫目录认领最新文件」的并发串图问题
- ✅ **已解决（v3.3.4）· 同族去重被兜底回填绕过**：`select_objects()` 的回填改成三档递进且每档都过同族检查（非容器不同族 → 非容器允许同族 → 容器项），枚数硬约束不变；被迫同族回填时日志明确警告「因候选不足，回填了同族元素 X」
- ✅ **已解决（v3.3.4）· `leaf pile` 类集合名词漏判同族**：`_family()` 新增 `COLLECTIVE_HEADS`（pile / heap / stack / cluster / bunch / bundle / bouquet…），这类中心词改用它前面「被数的那个东西」判族
- ✅ **已解决（v3.3.4）· `board` / `screen` 误命中案板与屏风**：多义词从单词级词表移出，改为 `TEXT_DEPENDENT_PHRASES` 按词边界做短语匹配（`display board` / `information board` / `display screen` …）
- ✅ **已解决（v3.3.4）· 卡纸元素编号行带数写死 6**：抽成 `DEFAULT_MAX_ELEMS` 并与 `--max-elements` 联动，行带数取「预期枚数」与「实际检出枚数」的较大值
- 📌 **两套面积阈值故意不统一**：卡纸 `MIN_ELEM_AREA_MM2=120` 是「值得上卡展示的最小面积」，印前 `--min-area 25` 是「视为有效元素的最小面积」，用途不同，已在两边代码加注释并加测试守护，勿强行统一
- ✅ **已解决（v3.4.0）· 成套装备 4 轮不收敛**：`ConvergenceGuard` 保证「同一物体连续 2 轮失败 / 一轮内撞互斥规则 → 永久剔除换候选」，复合体拆解改为「词库 → 模型 `composite_parts` → 通用中心词规则」三层。词库漏判现在只会多花 1~2 轮，不会全败。回归测试 9 项
- ⚠️ **`SIMILAR_FAMILIES` 是硬编码词库**：v3.3.3 已扩到 17 族（含树体/建筑/花三组部件-整体关系）并支持复数，但遇到新品类（如乐高、手办、宠物用品）仍会漏判，需持续补充
- ⚠️ **`gpt-image-2` 从未实测**：适配代码写完了，尺寸校验自测通过，但真实 API 调用一次都没跑过。拿到 key 后必须先跑 `selftest_provider.py --all`
- ⚠️ **火山 Seedream 未实测**：仅验证端点连通（401 正常返回）。模型 ID `doubao-seedream-4-0-250828` 可能已过期，需去控制台抄
- ⚠️ **Seedream 做不了 400dpi**：总像素上限约 462 万，A5@400dpi 需 771 万，超限。走火山只能 300dpi
- ✅ **已缓解（v3.4.0）· 300dpi 档余量 12px**：门禁改为公式推导并带 0.5% 量化容差 → 1739px，gpt-image-2 a5-300 档（1760px）余量从 12px 抬到 21px；能力表每次启动打印余量
- ⚠️ **`relayout.py` 只吃整版 sheet**，不吃多张分离 PNG。要开"代印档"需新增多图输入模式（约半小时工作量）
- ⚠️ **源照片分辨率**：建议短边 ≥1500px。实测 604px 也能出，但小物件细节丢失
- ⚠️ **RGB 非 CMYK**：交付文件是 RGB（不嵌输出 ICC），模切店须知已写明按 sRGB 解释 + RIP 端转换 + 首单打样确认
- ⚠️ **未成年人合规**：G0 会标记疑似未成年人，必须取得监护人书面授权才能接单

---

## 协作约定

每次完成任务后，AI 必须做三件事：
1. 更新本文件的"当前状态快照"
2. 在 `CHANGELOG.md` 顶部追加本次变更记录
3. 向用户汇报：①完成了什么 ②遇到了什么问题 ③下一步需要用户做什么

版本号规则：
- v0.x：调研/PRD 阶段
- v1.x：产线搭建（出图 + 质检）
- v2.x：印前与交付（刀线 / 拼版 / 数码模切）
- v3.x：迁移与商业化（Codex 迁移、真实订单）
- 补丁（x.y.z）：bug fix 或小调整

⚠️ **特别约定：在拿到第 1 个付费用户之前，不要主动提议新功能开发。** 优先级永远是验证 > 开发。

---

## 相关产出

（PRD、Prompt 实测报告、SOP、交付包等文档存放在项目所有者的私有云盘，未随仓库公开）

---

## 未来优化清单

- `relayout.py` 支持多张分离 PNG 输入（开"代印档"前置）
- 透明背景生图（`OPENAI_IMAGE_BACKGROUND=transparent`）可能提升元素分割精度，待实测
- AI / CDR / DXF 格式导出（店家若要求）
- 顾客自助上传 + 预览的轻量前端
