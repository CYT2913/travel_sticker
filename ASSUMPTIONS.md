# 我做的假设

按 AGENTS.md 2.4 记录。每条含：假设内容 / 依据 / 选错的影响 / 改回成本。

---

### 1. AGENTS.md 的第 0 节由我新建，而非「补齐」

- **假设**：飞书原文只有「提问纪律」一节，不存在第 0 节「项目信息」，也不存在编号 2.4。我保留原文全部文字作为第 2 节，新建第 0 节和第 1 节，并把原文末段「替代动作：写假设」编为 2.4 以对齐引用。
- **依据**：读取飞书文档 revision 3 全文，确认只有标题 + 提问纪律一节。
- **选错影响**：小。若你本意是另一份更完整的模板，只需把那份贴过来，第 0 节内容可整段平移。
- **改回成本**：低，5 分钟。

### 2. 移除了内部平台 provider，改为通用 `cmd` provider

- **假设**：你要求「避免盗用他人 skill 的风险」，因此公开仓库中不能保留任何调用公司内部 skill 的代码路径。我把原 `aime` provider（通过 subprocess 调用内部 `image-generate` / `analyze_media`）整体替换为通用 `cmd` provider，用 `FORGE_GEN_CMD` / `FORGE_VISION_CMD` 环境变量接任意外部命令。
- **依据**：原实现引用了公司内部 skill 的目录路径与调用约定，属他方资产；通用命令接口能力等价且不含任何他方信息。
- **选错影响**：中等偏低。公开仓库功能不减（openai / volcengine / cmd 三条通路齐全），但在内部环境跑需要显式配 `FORGE_GEN_CMD`。
- **改回成本**：低。工作区内部版本未改动，仍可直接用。

### 3. 默认 provider 从内部改为 `openai`

- **假设**：公开仓库的合理默认应是通用可获得的服务。
- **依据**：仓库面向外部 Agent 与你本人使用，内部 provider 已移除。
- **选错影响**：极小，一个环境变量。
- **改回成本**：一行。

### 4. 仓库不含任何客户原始照片与成品交付件

- **假设**：`*.jpg` `*.jpeg` `交付_*/` `run_*/` 全部 gitignore。`examples/` 只放 2 张生成后的贴纸版缩略图（无脸拼贴，不含可识别人像）。
- **依据**：原始照片含真实人物，属隐私；历史产物超过 1GB，不适合 Git。
- **选错影响**：小。若你要在 README 展示更多样例，补脱敏图即可。
- **改回成本**：低。

### 5. 银杏单保留「单片银杏叶 + 叶簇」两枚同族元素

> 更新（v3.3.3）：叶片与叶簇已在 `SIMILAR_FAMILIES` 中折叠为同一「树体族」，同族最多留 1 枚，此假设对**新跑的单**不再成立；已交付的旧成品不重跑。

- **假设**：不为此重跑。
- **依据**：该照片 G0 候选池仅 6 个物品，剔除同族后不足 6 枚，兜底回填。属照片本身可提取物少，非代码缺陷。独立目检结论为「一单体一簇属互补，可正常印刷」。
- **选错影响**：中。若你认为视觉上仍算重复，重跑一次即可，但候选池限制下大概率仍会撞。
- **改回成本**：一次生图（约 1 元 + 数分钟）。

### 6. 新增 `tests/` 而非沿用既有测试

- **假设**：项目原本没有任何测试，我新建了离线回归测试锁死三个历史缺陷。
- **依据**：仓库内未找到 test 目录、pytest 配置或 CI 配置。你的验收标准要求「能测试跑绿的写清测试」。
- **选错影响**：无负面。
- **改回成本**：不需要改回。

### 7. prompt 瘦身止步 5638 字符，未压到 4500

- **假设**：本次「prompt 总长压到 4500 以内」的目标按**压缩比**达成，而不是硬凑绝对值。实测原始基线是 **8631 字符**（日间/有人物/6 枚），不是需求里写的 ~6800；压缩后 **5638 字符（−34.7%）**，已略优于需求隐含的压缩比（6800→4500 = −33.8%）。
- **依据**：剩余 ~2900 字符全部是**功能性约束**，每一条都有实拍回退作依据：容器剔除（蛋糕+盘子重叠）、纪念物保护（蜡烛被截断）、IP 合规（Fender/知乎水印）、邻距与边距（模切安全）、粗度阈值（细杆切不了）。要再砍 1100 字符，只能删这些，或者削风格段——而需求明确要求**不得削减风格段**。风格段占比已从 38.2% 提到 49.6%，「让风格段占更大比重」这个真实意图已达成。
- **选错影响**：低。若你确实要硬性 ≤4500，可再砍 `LAYOUT`（489 字符，间距本来就由 `relayout.py` 代码硬保证，prompt 里这段只是减少重试轮数的冗余保险）。
- **改回成本**：低，10 分钟；但可能增加平均重试轮数，即增加生图成本。

---

## v3.3.3 技术债清理新增的假设（2026-08-30）

### 8. 文字依赖型物体只降权、不硬删

- **假设**：匾额 / 展板 / 告示牌 / 横幅这类「去掉文字后只剩纯色块」的物体，放进低优先级档而不是从候选池直接剔除；只有正常候选凑不满目标枚数时才回填。
- **依据**：命中判定是词级匹配，会误伤 `cutting board`（砧板）、`folding screen`（屏风）这类本身有价值的物体；硬删还会在候选池本就贫瘠的照片上（如银杏只有 6 个候选）触发凑不满枚数的连环重试。降权能在候选充足时达到同样效果，且不牺牲兜底能力。
- **选错影响**：低。最坏情况是某张照片候选实在太少，仍然回填了一枚空色块件 —— 但那种照片本来也没有更好的选择。
- **改回成本**：一行（把降权改成 `continue` 跳过）。

### 9. `make_memory_card.py` 的 `grab_elements()` 行序排序仍写死 6

- **假设**：不改。只把用于换算的贴纸幅面宽度 148mm 抽成了常量 `STICKER_SHEET_WIDTH_MM`，行序排序里的 `6` 留在原处。
- **依据**：这个 6 决定成品元素的**编号顺序**，而编号已经印在已交付的卡纸打印图和《卡纸说明》里，客户按编号找贴纸。改动排序逻辑会让新旧交付件的编号规则不一致，风险高于收益，且当前唯一在用的枚数就是 6。
- **选错影响**：低。若将来真要做非 6 枚的卡纸，需在这里同步改。
- **改回成本**：低（改成参数），但需连带核对编号预览图。

### 10. 目视质检返回不可解析 JSON 时判「本轮不合格」而非「跳过检查」

- **假设**：视觉模型回复解析不出 JSON，一律当本轮失败并触发重试。
- **依据**：原实现回退成 `{}`，等于所有门禁静默全过 —— 这正是 02 演出现场「5 枚被判双重质检全过」那类事故的放大器。宁可多花一轮重试，不能放行不可用成品。
- **选错影响**：低，最多多一轮生图成本；重试次数本身有上限保护。
- **改回成本**：一行。
